# Normalise line endings so formatters (Pint, Prettier) behave the same on
# Windows, macOS, Linux and CI.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary

*.blade.php diff=html
*.css diff=css
*.html diff=html
*.md diff=markdown
*.php diff=php
